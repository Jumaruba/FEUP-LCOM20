#ifndef __GRAPHIC_H
#define __GRAPHIC_H

#include <stdbool.h>
#include <stdint.h>
#include <lcom/lcf.h>
#include <minix/drivers.h>
#include <minix/com.h>

 
int init_mode(uint16_t mode);               //function to the first test, don't map and don't check mode 
int vbeModeInfo(uint16_t mode, vbe_mode_info_t *vmi_p);  
int drawPixel(uint16_t x_pos, uint16_t y_pos, uint32_t color); 
uint16_t get_hres();
uint16_t get_vres(); 
uint8_t get_bits_per_pixel();

#endif /* __GRAPHIC_H */
